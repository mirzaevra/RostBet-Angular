export interface Games {
  id?: number | string;
  ImageFullPath: string;
  Name: object;
  some: object;
}

export interface Categories {
  id: number;
}

export interface Merchants {
  id: number;
}
